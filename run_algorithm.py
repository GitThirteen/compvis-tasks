import src.task_1.algorithm as task1
import src.task_2.algorithm as task2
import src.task_3.algorithm as task3

# Call the algorithm through this file by uncommenting which one you want to run
# if you want to manually call the algorithm
#
# Python assumes that a called file lies at top-level apparently, so it won't check anything
# "above" the current folder unless you do hacky stuff, and since we need some imports
# from the util file above that, this is the easiest option.
# FOR DOING A FULL TEST RUN, USE THE TESTER.PY FILE INSTEAD!!!
if __name__ == '__main__':
    # task1.main()
    # task2.main()
    task3.main()
